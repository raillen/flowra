version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: kbsys-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-kbsys}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-kbsys_secret_password}
      POSTGRES_DB: ${DB_NAME:-kbsys}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - kbsys-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-kbsys} -d ${DB_NAME:-kbsys}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: docker/Dockerfile
    container_name: kbsys-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://${DB_USER:-kbsys}:${DB_PASSWORD:-kbsys_secret_password}@db:5432/${DB_NAME:-kbsys}?schema=public
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-jwt-key-change-in-production}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kbsys-network
    healthcheck:
      test: [ "CMD", "node", "scripts/health-check.js" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend (Nginx + Static files)
  frontend:
    build:
      context: ./frontend
      dockerfile: docker/Dockerfile
    container_name: kbsys-frontend
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
    depends_on:
      - backend
    networks:
      - kbsys-network
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:


networks:
  kbsys-network:
    driver: bridge
